# CHANGELOG

## 0.1.0

- 新增 `BoardColumn`：白底圆角容器，`title` + `content` 双插槽
- 新增 `BoardCard`：卡片容器，`title` + `description` + `onTap`
- 新增 `BoardView`：响应式多列布局，支持 flex 比例和桌面/移动自适应
- 重构：从旧版 Task 绑定组件改为纯结构组件，无业务假设
- 移除 `quanttide_project` 和 `flutter_quanttide` 依赖
